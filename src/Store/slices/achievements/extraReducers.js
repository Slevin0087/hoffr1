export const extraReducers = (builder) => {
    // builder.addCase(handleHints.fulfilled, (state) => {
    //   state.usedCounter += 1;
    //   storage.setItem(HINTS_STORAGE_KEYS.HINTS, state);
    // });
};